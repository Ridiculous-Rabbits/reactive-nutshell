import React from "react";
import PropTypes from "prop-types";
import { Link } from "react-router-dom";

const NewsButton = props => {
  // console.log(props)
  // if (props.buttonClicked === true) {
    return (
<button onClick={this.state.isPressedIn = true}>Add New News Article</button>
     
    );
  }
// };

// News.propTypes = {
//   // This rule ensures that `news` is passed a property
//   // and that is an object - not a string or number
//   news: PropTypes.object.isRequired,
//   id: PropTypes.number,
//   name: PropTypes.string
// }

export default NewsButton;
